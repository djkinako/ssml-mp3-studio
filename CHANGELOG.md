# 更新履歴

このファイルは SSML MP3 Studio の更新履歴。
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に準拠（`MAJOR.MINOR.PATCH`）:
- **PATCH** (0.0.x): バグ修正・微調整
- **MINOR** (0.x.0): 新機能・UI 変更
- **MAJOR** (x.0.0): 互換性のない破壊的変更

---

## [2.0.0] - 2026-06-15

### 🎉 公開版リリース (MAJOR)
個人用とは別に **キー不要・無料で使える「公開版」** を Firebase Hosting + Functions で構築。

### 追加(公開版 — `public/` + `functions/`)
- 🌐 **Firebase Hosting + Cloud Functions** で公開版を別ドメイン配信(`https://ssml-mp3-studio.web.app/`)
- 🔐 **API キーをサーバー側に隠蔽** — フロントには一切露出しない、ブラウザの View Source でも見えない
- 🚦 **IP ベース 1 日 4 回レート制限**(Firestore で `{IP}_{日付}` ドキュメント管理、JST 0:00 リセット)
- 📊 **残量(あと N 回)を画面に表示** — 各リクエスト後にサーバーから返却、localStorage キャッシュ
- 🎵 **Standard 声 11 種限定**(コスト抑制のため WaveNet/Neural2 は公開版で非対応)
- 🚫 **複数ブロック機能を意図的に除外** — 公開版は単一 SSML × 1 MP3 のシンプル運用
- 🛡️ Firestore Security Rules でクライアント直アクセス完全遮断、Admin SDK 経由のみ書き込み
- 📋 ファイル構成:
  - `firebase.json` Hosting + Functions + Firestore 設定、`/api/tts` を Functions に rewrite
  - `.firebaserc` プロジェクト ID `ssml-mp3-studio`
  - `firestore.rules` クライアントアクセス全遮断ルール
  - `functions/index.js` TTS プロキシ + buildSsml(`<lang>→<voice>` 変換、個人版と同じロジック)+ IP レート制限
  - `functions/package.json` Node.js 20 / firebase-admin + firebase-functions
  - `public/index.html` 公開フロント(キー入力欄なし、Standard 限定、単一 textarea、残量表示エリア)
  - `public/app.js` フロントロジック(localStorage 設定永続化、Fetch で `/api/tts` 呼出、残量キャッシュ)
  - `public/style.css` シンプルカード式、`.notice` カード(利用案内)、フッター

### 変更(個人版)
- 🏷️ 個人版も VERSION 1.2.0 → **2.0.0** にバンプして両者を同じバージョン番号で管理

### きなこ作業(デプロイ前に必要)
1. `npm install -g firebase-tools` (未導入なら)
2. `firebase login`
3. `cd functions && npm install`
4. Firebase Console で **Blaze プラン**へ切替(従量課金、無料枠あり)
5. **新しい Google TTS API キーを発行**(個人版とは別のキー、リファラ制限なし)
6. `firebase functions:secrets:set GOOGLE_TTS_API_KEY` で API キーを Secret として保存
7. `firebase deploy` で Functions + Hosting + Firestore を一括デプロイ
8. Google Cloud Console で**予算アラート設定**(月 $5 警告 / $20 停止)
9. (任意)カスタムドメイン設定

### 背景
v1.2.0 で Standard 声を追加 → きなこの実機試聴で「Standard で十分」判定 → 公開版に GO。
個人版(GitHub Pages)と公開版(Firebase Hosting)を**並行運用**する 2 軸構成に進化:

| | 個人版(現状継続) | 公開版(新規 v2.0.0) |
|---|---|---|
| URL | `djkinako.github.io/ssml-mp3-studio/` | `ssml-mp3-studio.web.app/` |
| API キー | ユーザー入力 | きなこのキー隠蔽 |
| 声 | 全種(Wavenet/Neural2/Standard) | Standard 限定 |
| ブロック追加 | 可(複数 SSML → 1 MP3) | 不可(1 SSML 固定) |
| 効果音 | あり(`audio/*.mp3`) | なし(シンプル運用) |
| レート制限 | なし | 1 IP / 1 日 4 回 |
| 想定ユーザー | きなこ自身 + IT寄り中国語学習者 | 中国語学習コミュニティ広く |

---

## [1.2.0] - 2026-06-15

### 追加
- 💸 **Standard 声 11 種を選択肢に追加**(日本語 4 + 中国語 7、各言語の `<optgroup>` で高品質系と分離)
  - 日本語: `ja-JP-Standard-A/B/C/D`(女女男男)
  - 中国語: `cmn-TW-Standard-A/B/C` + `cmn-CN-Standard-A/B/C/D`
- 💰 **声選択ヒント文に料金目安を明記**:
  - 高品質 Wavenet/Neural2: 月 100 万文字無料・超過後 $16/100万
  - 標準 Standard: **月 400 万文字無料・超過後 $4/100万**(きなこ用途で 4 倍の余裕)
  - SSML タグも文字数にカウント
- 🏷️ VERSION 1.1.1 → 1.2.0(MINOR、新機能)

### 維持
- デフォルト声は引き続き Wavenet 系(`ja-JP-Wavenet-B` / `cmn-TW-Wavenet-A`)
- 設定永続化(localStorage)で Standard 選択も自動復元

### 背景
きなこから「世の中に幅広く公開したい」「Standard で十分か試したい」と要望。Standard 声は WaveNet より機械的やが、料金は WaveNet の 1/4 + 無料枠 4 倍で、コミュニティ規模の公開運用に向く。Standard で実際に聴いてみてからデフォルト切替判断する想定。

### ⚠️ 一般公開する場合の注意(README/CLAUDE.md 反映予定)
- 静的サイトに API キー埋め込みは厳禁(ブラウザのソースで丸見え)
- 推奨は**ユーザーが自分の API キーを入れる現状の設計**を維持
- きなこのキーで全員に提供したい場合は Cloudflare Workers 等のバックエンド経由+レート制限が必須

---

## [1.1.1] - 2026-06-14

### 修正
- 🐛 **Markdown インライン強調記号(`**`/`*`/`__`/`_`/`` ` ``)5 種を解説中で禁止**。きなこ実機検証(Issue #3)で「TTS が文字通り『アスタリスク』『アンダースコア』『バッククォート』と読み上げてしまう」ことが判明したため
- 📋 `prompts/zhtw-v4.md` v4.6:
  - 「**禁止記号ルール**」セクション新設(NG 5 種の表 + 代替方法)
  - 自然に読み上げ可能な記号リストも明文化(括弧各種 / 矢印 / 中黒 / 三点リーダ / 波 / スラッシュ / 米印 / 星 / コロン)
  - 解説中の強調パターン推奨(キーワードは `「」`、概念ラベルは `『』` `【】`)
  - セルフレビュー 9項目 → **10項目**(Markdown 強調記号 NG チェック追加)

### 検証結果(Issue #3 きなこ実機テスト)
20 記号テストの結果:

| 結果 | 記号 |
|---|---|
| 🛑 読み上げられる(NG) | `**` `*` `__` `_` `` ` `` |
| ✅ 自然に処理される | `~~` `「」` `『』` `【】` `〈〉` `（）` `()` `→` `・` `…` `〜` `／` `※` `★` `:` |

特に意外だった発見: 米印 `※` も TTS は「米印」と読み上げない(注釈用記号として使える)

### 背景
v1.1.0 でワシ(Claude)が Issue #3 対応コメントの解説中で `**書類**` `**データ・統計値**` などの Markdown 太字記号を使ったところ、きなこから「アスタリスクって読み上げで入ってしまうので困る」と報告。Issue #3 で 20 記号網羅テスト SSML を生成 → きなこの判定結果を受けてルール化。今後は強調を全て `「」` 等の括弧系に統一。

---

## [1.1.0] - 2026-06-14

### 追加
- 🎬 **翻訳意図補足機能** をプロンプト v4.5 に追加。Netflix 字幕などの **意訳ペア・直訳から離れた翻訳** を入力したとき、解説に「直訳とどう違うか」「なぜそう訳されたか」を 1〜2 ポイント補足する
- 📋 `prompts/zhtw-v4.md` v4.5:
  - 「**翻訳意図補足ルール**」セクション新設(検出マーカー5種 + 解説テンプレ4ステップ)
  - 「解説の優先順位」を 5項目 → 6項目に拡張(成語の次に「翻訳意図の補足」を追加)
  - Few-shot 4「Netflix 字幕由来・意訳ペア」を追加(例文: 如果能靠音樂養活自己，誰不想？/ そりゃあね 音楽で食べていけたら それはね)
  - セルフレビュー 8項目 → **9項目**(翻訳意図の検出チェック追加)
- 📋 `.github/ISSUE_TEMPLATE/audio-lesson.yml` に **「ソース種別」ドロップダウン** 追加(オリジナル/Netflix字幕/ニュース/歌詞詩)。Netflix を選ぶと翻訳意図補足が強めに入る

### 検出される意訳パターン
- **反語表現の言い換え**: 中国語 `誰…?` / `哪有…?` → 日本語「そりゃあね」「もちろん」「みんな」
- **常用パターンの慣用句マッチ**: `靠X養活Y` → 「X で生計を立てる」「食べていく」
- **語順の組み替え**: 字幕の文字数制限と話者テンポで再構成
- **情報の追加・省略**: 字幕の読みやすさで補い/削り
- **口語化・話者キャラ反映**: カジュアル/敬体への振り

### 背景
v1.0.1 安定版確定後、きなこから「Netflix 字幕のような意訳ペアで、翻訳の意図を読み取って補足してほしい」と新規要望。N1〜N2 学習者にとって字幕は永遠に枯渇しない教材源であり、「直訳と違う!」モヤモヤを学習機会に変える機能。プロンプト v4 のフレームワークに完全に整合、アプリ層の改修不要。

---

## [1.0.1] - 2026-06-14

### 変更
- 🧼 **1 ブロック時の SSML 入力 UI を「元のシンプル UI」スクショに完全準拠**:
  - 「ブロック 1」ヘッダー + × 削除ボタンを **非表示**
  - 「合計 0 バイト / 1 ブロック」表示も **非表示**
  - 「＋ ブロックを追加」を **控えめなテキストリンク風ボタン**に
- 🔁 **2 ブロック以上にすると UI 自動展開**: ヘッダー復活・点線でブロック間区切り・合計バイト表示復活
- 💡 `:has(.ssml-block:only-child)` セレクタを使った CSS のみの実装、JS ロジック追加なし
- 🏷️ VERSION 1.0.0 → 1.0.1

### 背景
v1.0.0 でシンプル UI に戻したが、きなこから「元のスクショ」(v0.5.0 相当・複数ブロック装飾なし)に近づけてほしいと指示。**1 SSML 派には完全シンプル / 複数結合派には機能フル展開**の両立を CSS で実現。

---

## [1.0.0] - 2026-06-14

### 🎉 安定版リリース
今日 1 日で v0.1.0 → v1.0.0 まで爆速進化。実機検証 + バグ修正 + プロンプト v4 + 効果音 + 複数 SSML 結合 + UI 検証、全部やり切ったから **MAJOR バンプして安定版宣言**。

### 変更
- 🔙 **UI を v0.8.1 のシンプル版にロールバック**。v0.9.0 で試した「ノート手書きトーン v2」は、きなこの実機判断で「装飾盛りすぎ・シンプル方が使いやすい」と判定。シンプルカード式に戻す
- 🏷️ `app.js` の VERSION 0.9.0 → **1.0.0**

### 維持(機能フル装備のまま)
- 複数 SSML ブロック → 1 MP3 結合(v0.6.0)
- リピートセクション(v0.7.0)
- 効果音自動挿入(v0.8.0)
- プロンプト v4.4(プレースホルダ記号 + 解説直前マーカー)
- 効果音 24kHz mono 統一(v0.8.1)
- API キー + 声・速度 4 項目の localStorage 永続化
- GitHub Pages 公開

### 背景
v0.9.0 で Claude Design v2「ノート手書きトーン」(方眼ノート + スパイラル綴じ + マスキングテープ + 黄色マーカー + 関西弁ピル + Google Fonts 3 種) を一旦実装したが、実機検証で「シンプルな方が使いやすい」と判断 → v1.0.0 で元のシンプル UI に戻す。

**v0.9.0 のデザインは git history (`c356fb9`) に残るから、将来やっぱり使いたくなったら復活可能**。捨ててないで、ペンディング状態。

---

## [0.9.0] - 2026-06-14

### 追加・変更
- 🎨 **UI 全面リニューアル — Claude Design「v2 本命案・ノート手書きトーン」を採用**
- 🖋️ **方眼ノート風デザイン**: 緑の細罫線(27pxピッチ)+ ピンクのマージン縦線 + スパイラル綴じ + マスキングテープでバージョン表示。学習者の手元にあるノートに書き込む感覚を再現
- 🎭 **デザイントークン全面刷新**:
  - 背景: くすみベージュ `#c9cdbf` + 放射状ハイライト
  - ノート紙: アイボリー `#fbfdf6`
  - アクセント: 黄色マーカー `#ffe000` + ピンク `#ffb3d1`
  - メイン緑: `#00c853` (生成ボタン)
  - テキスト・枠線: ほぼ黒 `#23271a`
- 🔤 **Google Fonts 3 種**: Dela Gothic One(見出し)+ Yomogi(本文・手書き感)+ Zen Maru Gothic(ベース・丸ゴ)
- 🪧 **ネオブルータリスト系装飾**: 2.5px 太枠 + ボックスシャドウ(`3px 3px 0` の浮き上がり)
- 🔢 **番号バッジ**(円形・回転)で各セクションをラベリング
- 🟡 **黄色マーカー**でキーワード強調(`Studio` / 速度値 / セクション見出し)
- 🏷️ **関西弁ピル**: 「💪 ながら聴き教材」「🇯🇵＋🇹🇼 混在 OK」「📓 関西弁トーン」をヘッダーに配置
- 📀 メイン生成ボタンを **「📀 MP3 作るで」** に(関西弁化)
- 🎵 結果カードに薄いクリーム色 + アクセント

### 維持(機能は一切変更なし)
- 全機能 ID / `app.js` イベントハンドラ / 設定永続化 / 効果音挿入ロジック / SSML プロンプト v4.4 / バージョン管理ルール

### 背景
Issue #2「UI のブラッシュアップ」で、ワシが現状 UI 仕様書 12 章 Markdown を Claude Design に渡せる形にまとめ → きなこが Claude Design セッションで「音声読み上げ君」プロジェクトとして 2 デザイン提案を生成 → 「v2 ノート手書きトーン本命案」採用。学習ノート的なメタファーが「ながら聴き教材作成」のペルソナと完璧に合致したため。

---

## [0.8.1] - 2026-06-14

### 修正
- 🐛 **効果音再生で MP3 停止するバグを修正**。原因は MP3 フォーマット不一致(効果音: 44.1kHz JntStereo 128/192kbps + ID3v2.4 タグ / Google TTS: 24kHz mono 64kbps + ID3 なし)で、連結時にデコーダがフレームヘッダー切替で詰まっていた
- 🎵 効果音 `audio/explain.mp3` `audio/transition.mp3` を **24kHz Monaural 64kbps CBR・ID3v2 なし** (Google TTS 出力と完全一致) で再エンコード
- 🔧 `app.js`: TTS API リクエストの `audioConfig` に `sampleRateHertz: 24000` を明示。Google TTS の出力フォーマットを今後も 24kHz mono に固定して効果音と完全一致を保証

### 背景
きなこの実機検証で「効果音を再生する部分で MP3 が止まる」報告を受け、フォーマット不整合を特定。`file` コマンドで MP3 ヘッダー解析、`xxd` で ID3 タグの有無確認、ffmpeg で 24kHz mono への再エンコードという順で修正。

---

## [0.8.0] - 2026-06-14

### 追加
- 🔊 **効果音 SFX 自動挿入**: アプリ起動時に `audio/explain.mp3`(解説冒頭) と `audio/transition.mp3`(ブロック間) を fetch しキャッシュ。MP3 結合時に **解説直前マーカーと各ブロックの境目に効果音バイト列を自動挿入**
- 🪧 **解説直前マーカー** `<!-- explain -->` をプロンプト v4 に必須化。アプリ側は SSML をこのマーカーで 2 つに分割し、間に解説前効果音を挟む
- 📢 **プレースホルダ記号の読み上げルール**: 解説中の「〜」「~」「○○」などは **「なになに」** に、中国語パートの「…」「~」などは **「什麼什麼」** に置換する明確なルール化。TTS で記号が黙読される問題を解消
- 📋 `prompts/zhtw-v4.md` v4.4: 上記ルールを反映、Few-shot 3例の解説直前に `<!-- explain -->` マーカー追記、セルフレビュー 6項目 → **8項目**(プレースホルダ記号・マーカー追加)
- 🎵 `audio/explain.mp3` (15 KB) `audio/transition.mp3` (37 KB) をリポジトリに同梱

### 変更
- 📊 進捗表示で `<!-- explain -->` で分割されたパートは `ブロックX-1` `ブロックX-2` のように表示
- 📊 ステータスに `(効果音入り)` / `(効果音無し)` 注釈追加。効果音ロード失敗時も MP3 生成自体は続行する

### 背景
v0.7.0 のリピート確認後、きなこから 4 つの追加要望(プレースホルダ記号読み上げ・ブロック間効果音・解説前効果音)を一括受領。SSML レベル + アプリレベルの組み合わせ実装で全部対応。

---

## [0.7.0] - 2026-06-14

### 追加
- 🔁 **リピートセクション** をプロンプト v4 の必須構造に追加。解説終了後に **「ほな、もう一度聞いてみよか。」** のような関西弁ブリッジ + **正解の中国語文を 2 回再生**(間 1.5 秒)が入る
- 📋 `prompts/zhtw-v4.md` v4.3:
  - 「## SSMLの全体構造」: 6項目 → **7項目** (リピート追加)
  - 「## SSMLの記法ルール」: ポーズ目安にリピート関連(解説後 2s、ブリッジ後 800ms、リピート間 1.5s)を追記
  - Few-shot 3 例(凱旋而歸/好像會下雨/光說不練)を全部リピート部分込みに更新
  - セルフレビュー 5項目 → **6項目** (リピート完備チェック追加)

### 背景
v0.6.0 リリース直後の実機検証で「3 ペアの結合 MP3 が出た」と確認後、きなこから「解説の後にもう一度中国語を 2 回読み上げてほしい(『ではもう一度文章を聞いてみましょう』)」と新ルール要望。学習音声として正解を耳に焼き付けるリピート再生は、ながら聴きで定着率が大きく上がるため即時対応。

---

## [0.6.0] - 2026-06-14

### 追加
- 🎯 **複数 SSML ブロック対応**: UI の SSML 入力エリアを「ブロック」単位に変更。**「+ ブロックを追加」ボタンで動的に textarea を増やせる**(削除も可、最低 1 ブロック)。各ブロックに独立した SSML を貼り付ける
- 🔗 **複数 MP3 自動結合**: 「🔊 MP3 生成」押下時、全ブロックを順次 Google TTS API に投げて MP3 を取得 → **`Uint8Array` で単純連結して 1 つの MP3 として DL**。ffmpeg / Web Audio API 不要、MP3 フレームの独立性を利用
- 📊 各ブロックの個別バイトカウンタ + 全ブロック合計バイトカウンタ表示
- 📈 進捗表示: 「ブロック X を生成中… (i/N)」のように順次状況を表示

### 変更
- 📋 `prompts/zhtw-v4.md` v4.2: v0.5.0 で入れた「全ペア統合」ルールを廃止し、**v0.4.0 ルール(ペアごとに独立した `<speak>`)に戻す**。理由: アプリ側で MP3 結合機能を持ったため、SSML レベルでの統合は不要。各ペアを 5000 バイト上限内のまま最高品質で出せる
- 📋 `CLAUDE.md` の Issue 対応コメントフォーマット例も「ペアごと別 `<speak>` + 各ブロックに貼る運用」に更新

### 背景
v0.5.0 では「複数ペアを 1 つの `<speak>` に統合」というプロンプトレイヤーの対応を入れたが、Issue #1 の 3 ペア依頼で **物理的に統合できない**(合計 8358 バイトで 5000 上限の 1.7 倍)ことが判明。アプリ側で「複数 SSML → 1 MP3」を解決する方が本質的かつスケーラブルと判断し、v0.6.0 で実装。プロンプト v4 のシンプルさも保てる。

---

## [0.5.0] - 2026-06-14

### 変更
- 🎯 **複数ペアの SSML を 1 つの `<speak>` に統合**するルールに変更(ssml-mp3-studio が 1 SSML = 1 MP3 として処理する前提に合わせる)。ペア間に「次いくで。」のような区切りアナウンス + `<break time="4s"/>` を挟む
- 📋 `prompts/zhtw-v4.md` v4.1: 「複数ペア統合の構造テンプレート」「SSML サイズ制限への対応(5000バイト超は自動分割+明示)」「セルフレビュー6項目(`<speak>` 統一・サイズ上限チェックを追加)」を新設
- 📋 `CLAUDE.md` の Issue 対応コメントフォーマット例を統合版に更新、5000バイト超時の分割表示ルールを追記

### 背景
v0.4.0 リリース後の初回ユーザーテスト(Issue #1)で、3 ペア依頼に対して各ペアを別 `<speak>` で返したところ「複数ペアは 1 MP3 にまとめたい」とフィードバック。ssml-mp3-studio は SSML 入力欄が 1 つで 1 `<speak>` 前提のため、ペアを統合する方が UX として正しい。

---

## [0.4.0] - 2026-06-14

### 追加
- 🎧 **Issue 起点 SSML 生成ワークフロー** を導入。日本語+台湾華語(繁体字)ペアを GitHub Issue で投げると、Claude Code が `prompts/zhtw-v4.md` を使って段階的中国語化 SSML を生成し Issue にコメント返信する
- 📋 `prompts/zhtw-v4.md` — Web 版 Claude セッションで 4 ループ検証した台湾華語学習教材 SSML 生成プロンプト v4 を配置。N1〜N2 学習者向け・関西弁トーン・成語解説・5項目セルフレビュー指示込み
- 📋 `.github/ISSUE_TEMPLATE/audio-lesson.yml` — 「🎧 中国語学習音声 SSML 生成依頼」Issue テンプレ
- 📋 `CLAUDE.md` に Issue 起点ワークフローの手順を明文化(Claude Code の責務範囲・コメント形式・守るべきこと)
- 📋 README に「学習教材を作る(Issue 起点ワークフロー)」セクション追加

### 背景
Web 版 Claude セッションで「Multi-Agent + GitHub Issue + ffmpeg + Drive 統合」のフル仕様 (zhtw-audio-lab) 構想を立てたが、4視点 (architect/critic/engineer/UX) でレビューした結果、「ssml-mp3-studio v0.3.0 が既にコア論点を解決済みのため申し送り書の大部分は過剰」と全員一致。プロンプト v4 を ssml-mp3-studio に同居させ、Issue 起票 → SSML 生成 → ブラウザで MP3 化 のシンプルワークフローに集約。

---

## [0.3.0] - 2026-06-14

### 追加
- 🎯 **設定の永続化**: API キーに加えて、声 2 つ・速度 2 つも localStorage に保存。ブラウザを閉じても次回開いた時に前回の設定が復元される
- 🏷️ **バージョン表示**: ヘッダーの見出し脇に現在のバージョン(`v0.3.0`)を表示
- 📝 **CHANGELOG.md** を新規作成。過去バージョンの履歴も遡及して記録
- 🌐 **GitHub Pages デプロイ**: 公開可能な状態に整備

---

## [0.2.0] - 2026-06-14

### 追加
- 🇯🇵🇹🇼 **日本語の声と中国語の声をそれぞれ独立して選択可能**に（プルダウン 2 つに分離）
- 🐢🐇 **日本語の速度と中国語の速度を独立スライダー化**（各 0.5〜1.5）
- 🥟 中国語の声のデフォルトを **台湾華語 (`cmn-TW-Wavenet-A`)** に変更（大陸普通話も選択肢として残置）

### 変更（内部）
- 🪄 **`<lang>` → `<voice>+<prosody>` 自動変換ロジック** (`buildSsml()`) を導入。Google TTS の `<lang>` タグが "best effort" で日本語声に中国語を読ませると無視される問題を、アプリ側で吸収。ユーザーは今まで通り Claude が出す `<lang>` 入り SSML を貼るだけで OK
- 📊 速度は入れ子 `<prosody>` で外側=ja・内側=zh/ja の相対値とし、ユーザー指定値がそのまま絶対速度として実音に反映されるよう調整

### ドキュメント
- README に「仕組み（なぜ `<lang>` で動くのか）」の節を追加
- SPEC §5 注釈を実機検証結論で更新、§6 サンプルを繁体字に統一、§7 UI 要素を 4 項目構成に更新

---

## [0.1.2] - 2026-06-14

### 修正
- 🧹 `.gstack/` を `.gitignore` に追加（ローカル状態ディレクトリをリポに含めない）

---

## [0.1.1] - 2026-06-14

### 修正
- 🐛 **生成前に再生プレビュー / DL ボタンが表示されるバグを修正**。CSS 詳細度で `#result`(ID) の `display: flex` が `.hidden`(class) の `display: none` に勝っており、生成前から空の `<audio>` と無効 DL ボタンが見えていた。`.hidden` に `!important` を付与して確実に隠す

---

## [0.1.0] - 2026-06-14

### 初版
- ✨ MVP 足場: SSML テキストを貼って、Google Cloud Text-to-Speech API で MP3 を生成・ダウンロード
- 🔑 API キー入力欄 + localStorage 保存
- 🗣️ 声の選択（日本語 Wavenet/Neural2、中国語 cmn-CN/cmn-TW）
- 🎚️ 読み上げ速度スライダー（0.5〜1.5）
- 📋 SSML サンプル投入ボタン + バイト数カウンタ（5000 バイト上限警告）
- 🎵 生成後の再生プレビュー + ダウンロードボタン
- 📄 SPEC.md / README.md 同梱
