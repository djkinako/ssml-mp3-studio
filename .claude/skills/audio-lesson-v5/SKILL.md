---
name: audio-lesson-v5
description: |
  台湾華語(繁体字)学習用の SSML モジュール式教材を生成する。
  Issue で「プロンプトモード = v5-modular」が選択されている場合に起動。
  Phase 0(フォーカス語抽出) → Phase 1-4(ベース+漢字認識+語彙+文法)を
  ペアごとに生成し、投稿前に必ず lint.sh を実行してルール違反ゼロを確認する。
when_to_use: |
  ユーザーが Issue #X に対して「対応して」「v5-modular で生成して」と依頼した時、
  または Issue 本文に「プロンプトモード: v5-modular」が含まれる時。
  v4(シンプル全部入り)が指定されている場合は、このスキルは起動せず
  `prompts/zhtw-v4.md` を直接読むフローへ抜ける。
tools_needed: [Read, Bash, Grep, Edit, Write]
---

# audio-lesson-v5 — 決定論的 7 ステップフロー

このスキルは **「面倒な手順を踏み外したら絶対にコメント投稿まで進ませない」** ための強制力を持つ。
Step 1 〜 Step 7 を **順番通り** 実行する。途中ステップをスキップ・並び替え・省略してはならない。
各 Step の冒頭に「やること」「使うツール」「成功条件」「失敗時の対処」「真因対策」を明記する。
真因対策の Q1-Q7 は別エージェント検証で表面化した7つの構造的失敗パターン(Q1=画数表記スリップ / Q2=Phase 0 微修正 / Q3=単語分割ブレ / Q4=鬼門ワード再発 / Q5=Issue 括弧ふりがな未反映 / Q6=機能語誤配置 / Q7=lint 未実行告白)に対応している。

---

## Step 1: Issue 取得

### やること
1. ユーザーが指定した Issue 番号を取得(チャットで「#15 対応して」等)
2. `gh issue view <number> --comments` で Issue 本文 + 既存コメント全部取得
3. Issue 本文の **プロンプトモード** フィールドを確認
   - 「v5-modular(モジュール式・推奨)」→ このスキルを継続
   - 「v4(シンプル全部入り)」→ **このスキルを終了**し、`prompts/zhtw-v4.md` を読む v4 フローへ抜ける
4. **出力モジュール選択** チェックボックスを確認
   - 全部未チェック または 全部チェック → 全 4 モジュール(Phase 1-4)出力
   - 一部だけチェック → そのモジュールのみ出力
5. **ソース種別** を確認(Netflix・字幕 → Phase 1 で翻訳意図補足を強める)
6. **考える時間(ヒント後のポーズ)** を確認(3秒 / 4秒 / 6秒)
7. **メモ** フィールドの特別指示を確認(例: 「把構文厚めに」)

### 使うツール
- `Bash`: `gh issue view <number> --comments`
- `Read`(必要なら): 添付画像があれば `gh api <attachment_url> > /tmp/issueN_M.png` で DL → Read

### 成功条件
- Issue 本文がパースでき、プロンプトモード = v5-modular であることが確認できた
- 出力モジュール / ソース種別 / ポーズ秒数 / メモ が変数として手元にある

### 失敗時の対処
- Issue 取得失敗 → Issue 番号が正しいか確認、`gh auth status` で認証確認
- プロンプトモードが v4 → 即座にこのスキルを終了し、v4 フローへ案内
- 必須項目「例文ペア」が空 → ユーザーに記入を要求してスキル終了

### 真因対策
- **Q5 対策の準備**: Issue 本文の括弧ふりがな(「博多(はかた)」「元(げん)」)を Step 2 で抽出するため、本文をそのまま記憶に保持する

---

## Step 2: 入力パース

### やること
1. Issue 本文の「例文ペア」フィールドを `---` 区切りで分割し、N 個のペアにする
2. 各ペアを「日本語: 〜 / 中国語: 〜」の 2 行構造でパース
3. **話者名カッコ削除**:
   - 「(リーム)」「(ドアの開閉音)」「(BGM)」「(SE)」などの **話者名・効果音注記カッコ** を SSML 本文から削除
   - これらは Phase 1 の例文/正解で読み上げ対象から除外(残すと TTS が不自然に読む)
4. **Issue 括弧ふりがな抽出(Q5 対策の核心)**:
   - 日本語側に「博多(はかた)」「元(げん)」「中田(なかた)」のように **漢字 + 全角括弧ひらがな** が出現していたら、それを **`<sub alias="ハカタ">博多</sub>`** 形式で SSML に **強制反映**するフラグを立てる
   - 半角括弧 `(はかた)` も同じく対象
   - これは Phase 1 の例文・ヒント・正解で TTS が誤読しないようにするためで、**省いてはならない**(別エージェント Q5 検証で全件スリップした要対策ポイント)
5. ペア配列 + 括弧ふりがな辞書を内部メモに保持

### 使うツール
- 内部の文字列処理(LLM 推論)
- `Edit` / `Write`(内部メモを `/tmp/v5-skill-state.md` に書き出して進捗管理してもよい)

### 成功条件
- N 個のペア配列が手元にあり、各ペアに「日本語生テキスト / 中国語生テキスト / 括弧ふりがな辞書」が紐づいている
- 話者名カッコ・効果音注記カッコは削除済み(SSML には混入しない)

### 失敗時の対処
- パース失敗(`---` 区切りが不正、日本語/中国語ラベル欠落) → Issue にコメントで「フォーマットが不正です」と通知してスキル終了
- 括弧ふりがな辞書が空 → そのまま継続(後続 Phase で `<sub>` 強制が発火しないだけ)

### 真因対策
- **Q5 対策完遂**: 括弧ふりがな辞書を必ず Phase 1 の例文/ヒント/正解の `<sub alias>` に反映する仕組みをこの Step で確立

---

## Step 3: ペアごとのループ開始

### やること
1. ペア配列をループし、ペア i = 1, 2, ..., N について Step 4 → Step 5 → Step 6 を順番に実行
2. ペア i の処理が **lint 全パス(Step 6 成功)** するまで次のペアに進まない
3. 全ペア処理完了後に Step 7 へ

### 使うツール
- 制御フロー(LLM 推論)

### 成功条件
- 各ペアについて Phase 1-4(または選択された一部)の SSML が lint 全パスで出来上がっている

### 失敗時の対処
- 特定ペアで lint が 3 回連続失敗 → ユーザーに「ペア i は lint が通らず難航中、入力テキストを見直すか手動レビュー希望か」を確認

### 真因対策
- **ペア単位の隔離**: 後のペアが先のペアに影響する変な相互作用を防ぐ。1 ペアずつ完結させる。

---

## Step 4: Phase 0 — フォーカス語抽出

### やること(ペア i に対して)
1. `prompts/zhtw-v5-modular.md`(スキル配下の改訂版)を **Read** する
   - パス: `.claude/skills/audio-lesson-v5/prompts/zhtw-v5-modular.md`
   - すでに同セッション内で Read 済みなら再 Read 不要(キャッシュ可)
2. 中国語例文から **3〜5 単語(原則 4 単語)** を抽出
3. 抽出基準(プロンプトの「Phase 0 抽出基準」優先度順)に従う:
   - 繁体字特有 / 日本語と字形が違う(這、實、亂、歸、們、麼…)
   - 日本語と意味/用法が違う(歸 = 書面語、們 = 複数マーカー)
   - 文の意味のコア(キー名詞・キー動詞・成語)
   - 派生・応用が広い(他の熟語にも展開可能な字を含む単語)
4. **必ず「単語/熟語単位」**(文字単位 NG、Q3 対策)
   - ✅ 「我們」「凱旋」「凱旋而歸」/ ❌ 「我」「們」「凱」「旋」
   - 例外: 一文字単語(「而」「歸」「了」など)はそれ自体が独立単語なので OK
5. **機能語(吧/了/的/呢/嗎/啊/啦)は 4 枠対象外**(Q6 対策)
   - これらは Phase 4(文法)の「語気助詞・文末詞」トラックで扱う
   - Phase 0 のフォーカス語に入れない
6. **グレーゾーンは一括側に倒す**(Q3 対策)
   - 「凱旋而歸」と「凱旋」が両方候補に上がったら **「凱旋而歸」一括** にする
   - 「放馬過來」「七上八下」など成語/慣用句は **分割せず一括**

### 自己相互チェック 3 点(Q2 対策、絶対実行)
抽出した 4 単語に対して以下を **自分で順に確認**:
1. **部分文字列共有なし**: A ⊂ B(A が B の部分)になっていないか
   - 例: 「搞清楚」と「搞」を両方ピックアップは NG。どちらか一方だけにする
2. **同一品詞・意味カテゴリの重複なし**
   - 例: 「我們」と「你們」を両方ピックアップは NG(両方とも人称複数代名詞で重複学習)
3. **Phase 3 派生語列挙が交差しないか**
   - 例: 「歸」のピックで派生に「歸來/歸國」を挙げる予定なら、「來」「國」を別ピックに入れない

### 文字数閾値と個数調整
- 例文 8 字以下 → 3 枠
- 例文 9〜15 字 → 4 枠(基本)
- 例文 16 字以上 → 4〜5 枠

### サマリ表の表出力(Q2 対策、必ず内部メモに残す)【★v1.5.1 改訂:5列統一・「選定理由」列名固定★】

抽出結果を以下の **5 列フォーマット** で内部メモ(`/tmp/v5-skill-state.md` または LLM 内部記憶)に保存:

```
| # | 単語 | ピンインカタカナ | 日本語訳 | 選定理由 |
|---|------|----------------|---------|----------|
| 1 | 我們  | ウォーメン       | 私たち   | 繁体字特有「們」、人称複数の最頻出ワード、派生広い |
| 2 | 凱旋  | カイセン         | 凱旋     | 難読「凱」、文のコア、日本語と共通熟語で覚えやすい |
| 3 | 而    | アー             | しかして | 書面語マーカー、現代台湾華語の文章理解に必須 |
| 4 | 歸    | グイ             | 帰る     | 繁体字の旧字体、口語「回」との対比軸 |
```

**列名 5 つ固定(改訂)**:
1. `#` — 連番(1〜4)
2. `単語` — フォーカス語そのもの
3. `ピンインカタカナ` — カタカナ表記(LLM 推論)
4. `日本語訳` — 短い訳語(1〜5字目安)
5. `選定理由` — なぜこの単語を選んだのか(短文 1 つで簡潔に)

**v1.5.1 改訂点**:
- 旧 v1.5.0 までは「抽出根拠(短く)」と表記していたが、列名衝突解消のため **「選定理由」で全 4 ファイル(プロンプト/SKILL.md/Few-shot 3 つ)統一**
- 「抽出根拠」「抽出理由」表記は撤回。今後は「選定理由」のみ使用
- lint PATTERN_S5 (--strict) で「選定理由」ヘッダ正規表現を固定検証(差分検出のセーフティネット)

このサマリ表は **Step 7 のコメント本文の「ペアごとのセクション冒頭」に貼る**(きなこ視認用 + 「微修正」しないための見える化)。
さらに **コメント本文の最上部** には「横断簡易表(4 列・ペア横断)」を別途出力する(Step 7 参照、★v1.5.1 改訂:二重表問題の解消★)。

### 使うツール
- `Read`: `.claude/skills/audio-lesson-v5/prompts/zhtw-v5-modular.md`
- 内部処理(LLM 推論)

### 成功条件
- 4 単語(または 3 / 5)が決まっていて、3 つの自己相互チェックを全部パスしている
- サマリ表(5 列「選定理由」フォーマット)が内部メモに記録されている

### 失敗時の対処
- 自己相互チェック 1 で部分文字列共有検出 → 該当ペアの片方を別単語に差し替えて再チェック
- 4 単語決まらない(候補不足) → 3 単語で確定して継続

### 真因対策
- **Q2(微修正)対策**: サマリ表を冒頭出力することで、後続の Phase 2/3 が「Phase 0 の抽出を勝手に変える」誘惑を構造的に潰す
- **Q3(分割ブレ)対策**: グレーゾーンは一括側ルールを明文化
- **Q6(機能語)対策**: 4 枠対象外を明文化
- **v1.5.1 列名統一**: 「選定理由」固定化で 4 ファイル間の表記揺れを撲滅(Critical #3 反映)

---

## Step 5: Phase 1-4 生成

### Few-shot 適用条件 7 軸(★v1.5.1 新規:High #7 反映:あいまい解消★)

ペア i の特性を **下表 7 軸** で判定し、該当する Few-shot のみを **主参照** として `Read` する。
重複適用 OK(最大 2 つの Few-shot を同時に主参照)。該当ゼロなら本体プロンプトのみで生成(Few-shot は副参照しない)。

| 軸 | 特性 | 適用 Few-shot | 例 |
|---|---|---|---|
| A | 四字成語含む | pair1-frieren | 凱旋而歸 / 七上八下 / 放馬過來 |
| B | 書面語スイッチ(而/之/其/以) | pair1-frieren | 而 / 歸 / 之 / 以 |
| C | 語気助詞(吧/啊/啦/欸/嗎) | pair2-conversation | 來吧! / 你啊? / 走啦 |
| D | 量詞特殊用法(把/支/隻) | pair2-conversation | 帶把傘 / 一支筆 |
| E | 動詞アスペクト(了/著/過) | pair2-conversation | 了啦 / 看著 / 去過 |
| F | 字幕意訳・反語(誰○○/哪有○○) | pair3-subtitle | 誰不想 / 哪有那麼簡單 |
| G | 翻訳意図補足(語順入替) | pair3-subtitle | 中→日語順入替の字幕特有 |

**判定手順**:
1. ペア i の中国語例文をスキャン
2. 上記 7 軸のうち該当するもの全てをリストアップ
3. 該当軸が指す Few-shot を **主参照**(最大 2 つ)として `Read`
4. 該当ゼロ → Few-shot 副参照しない、本体プロンプトのみで生成
5. Read 証跡(どの Few-shot を読んだか)は **チャット応答にのみ出力**、Issue コメント本文には出さない(★v1.5.1 改訂:High #8 反映:配置責任明確化★)

**重複適用例**:
- ペア「我們凱旋而歸」 → 軸A(凱旋而歸:四字成語)+軸B(歸:書面語) → pair1-frieren を主参照(2 軸とも同じ Few-shot)
- ペア「來吧!帶把傘」 → 軸C(吧:語気助詞)+軸D(把:量詞特殊) → pair2-conversation を主参照(2 軸とも同じ Few-shot)
- ペア「誰不想凱旋」 → 軸A(凱旋:成語化要素)+軸F(誰不想:反語) → pair1-frieren + pair3-subtitle 両方主参照

**「良いとこ取り」表現は廃止**(★v1.5.1 改訂)。Few-shot は「参照する/しない」の二択判定に明示化。

### やること(ペア i に対して、Step 1 で確認した選択モジュールのみ)

#### Phase 1: ベース SSML
1. `prompts/zhtw-v4.md` のルールを継承(段階化・動詞句単位・解説優先順位・関西弁トーン)
2. **Issue 括弧ふりがな辞書を `<sub alias>` に反映**(Step 2 で抽出したやつ、Q5 対策)
   - 例: Issue に「博多(はかた)」 → SSML 内の「博多」を `<sub alias="ハカタ">博多</sub>` に書き換え
3. **話者名カッコ・効果音注記** は SSML 本文に含めない(Step 2 で削除済み)
4. **ヒント段階で `<lang>` 3 連続禁止**(段階化バランス保持)
5. ソース種別 = Netflix・字幕 なら **翻訳意図補足**を解説で強める
6. **v5-modular 時は末尾リピートを 1 回**(v4 は 2 回、v5 は他モジュールが続くので 1 回で十分)

#### Phase 2: 漢字認識モジュール【v1.5.0 改訂:3パターン判定固定】

**真の責務**:
このモジュールは「ながら聞き」音声学習で「あ、あの字ね」と即座に思い出せる **認識** を提供する。
構成解説(部首・部品分解)は紙とペン書取練習で初めて意味を持つので、ここでは **やらない**。
書取は音声では不可能。やるべきは「日本語の既知字と紐づける」ことだけ。

**生成手順**:
1. Phase 0 の 4 単語を **同じ順序** で扱う
2. 各単語の構成字について「**3パターン判定**」を行う:
   - **① 日本語と同じ字** → 「『○○』の『○』と同じ字」
     - 既知熟語経由で紐づける(我 → 「我々」の「われ」)
   - **② 日本語新字体の旧字体** → 「日本語の『○る/○』の旧字体や」
     - 新字体経由で紐づける(歸 → 「帰る」の旧字体)
   - **③ 日本語にない繁体字** → 「日本語にない繁体字で、○○や」
     - 機能/意味だけ一言マーカー(們 → 複数形を作る接尾辞)
3. 単語あたり **1-2 文で完結する固定テンプレ**:
   ```
   ひとつ目、[単語](ピンインカタカナ、訳)。
   [字1のパターン判定]、[字2のパターン判定]。
   [必要なら全体補足 1 文]
   ```
4. **Issue 括弧ふりがな辞書を `<sub alias>` に反映**(Phase 1 と同じ)
5. **冒頭は関西弁見出し音声に統一**(28 ブロック中 21 回重複問題の解消):
   ```
   <break time="2s"/>漢字の認識モジュール、はじめるで。<break time="800ms"/>
   ```

**禁止事項(エージェント裁量ゼロ化、v1.5.0 で構造的削除)**:
- **部首名**(ニンベン / サンズイ / ガンダレ / まだれ / しんにょう / くさかんむり / うかんむり / きへんに / もんがまえ など、一切 NG)
- **構成位置**(上に○ / 下に○ / 左右に○ / 上下に○ / 真ん中に○ など、一切 NG)
- **古字部品の `<sub alias>` 指定**(`<sub alias="アニ">豈</sub>` 等、一切 NG)
- **印象表現**(ごっつい / 線多め / 画数モリモリ / ぎゅっと / がっつり / 骨太 / 重め / 濃い字、一切 NG)
- **字形描写**(縦長 / 横広 / 三層構造 / 格子状 / 左右対称 / シャープ、一切 NG)
- **「組み合わせる」「並べる」「重ねる」**などの構成動詞、一切 NG
- **画数情報**(○画 / 画数 / 点が N 個、Q1/Q4 対策、一切 NG)
- **鬼門ワード**(画 / 字義 / 然り而して / 豈 / 几 / 疋、Q1/Q4 対策、地の文に裸出し NG)
- **二度読み**(漢字 + 全角括弧ふりがな併記、ルール C、NG)

**一文字単語の例外枠**:
- 「これ一文字単語の例外枠で取り上げるで」前置きは維持
- ただし構成解説はせず、**3パターン判定のみ**行う
- 例: 「而」→「日本語の『而』と同じ字、書面語の接続詞や」(① パターン)
- 例: 「歸」→「日本語の『帰る』の旧字体や、書面語向きの言い回しに使うで」(② パターン)

**3パターン判定 早見表**:

| パターン | 対応字例 | 言い方テンプレ |
|---|---|---|
| ① 日本語と同じ字 | 我 / 材 / 線 / 凱 / 旋 / 而 / 止 / 自 / 木 / 工 / 夫 | 「『○○』の『○』と同じ字」 |
| ② 日本語新字体の旧字体 | 歸 / 實 / 亂 / 關 / 當 / 兒 / 榮 / 壹 / 萬 / 獨 | 「日本語の『○る/○』の旧字体や」 |
| ③ 日本語にない繁体字 | 們 / 啦 / 咧 / 嘅 / 咁 / 啲 / 嘢 | 「日本語にない繁体字で、○○や」 |

**生成例(凱旋而歸)**:
```
ひとつ目、我們(ウォーメン、私たち)。
「ウォー」は日本語の「我々」の「われ」と同じ字、
「メン」は日本語にない繁体字、複数形を作る接尾辞や。

ふたつ目、凱旋(カイセン、凱旋)。
「カイ」は日本語の「凱旋」の「がい」、「セン」は「旋回」の「せん」と同じ字や。
日本語と同じ熟語やから組み合わせで覚えられる。

みっつ目、書面語の主役 而(アー)。
日本語の「而」と同じ字、書面語の接続詞や。
日常会話では使わへんけど、文章では超頻出やで。

よっつ目、歸(グイ、帰る)。
日本語の「帰る」の旧字体や。意味は同じく「帰る」、書面語向きの言い回しに使うで。
```

#### Phase 3: 語彙深掘りモジュール
1. Phase 2 と **完全に同じ 4 単語、同じ順序** で扱う
2. 各単語について意味・派生・用法を解説
3. **派生語は 1 単語あたり最大 4 個**(ピンインカタカナは省略可)
4. **Phase 2 冒頭と同様の見出し音声**:
   ```
   <break time="2s"/>語彙の深掘りモジュール、はじめるで。<break time="800ms"/>
   ```

#### Phase 4: 文法モジュール
1. 構文軸で **最大 3 ポイント**(短文・淡白なら 1〜2 ポイントで OK、Q6 対策)
2. **「文法ポイントは[N]つや」を動的化**(3 固定 NG、Q6 対策)
   - 2 ポイントなら「文法ポイントは二つや」
   - 1 ポイントなら「文法ポイントを見ていくで」
3. **機能語(吧/了/的/呢/嗎/啊/啦)** をここで扱う(Phase 0 で対象外にしたぶん、ここに集約)
4. Phase 3 で扱った単語と被らせない
5. 冒頭見出し音声:
   ```
   <break time="2s"/>文法ポイントの解説や。<break time="800ms"/>
   ```

### 共通ルール
- SSML 形式: 各 Phase は独立した `<speak>...</speak>` で完結
- `<lang xml:lang="cmn-TW">` の前後に `<break time="200ms"/>`(直後句読点なら省略可)
- `<!-- explain -->` マーカーを各モジュールの解説部直前に必ず挿入
- Markdown 強調 `**` `*` `__` `_` `` ` `` は **絶対 NG**
- 末尾リピート完備(モジュール末尾に「もっかい」ブリッジ + 中国語例文再生)

### 使うツール
- `Read`: `.claude/skills/audio-lesson-v5/prompts/zhtw-v5-modular.md`
- `Read`: `.claude/skills/audio-lesson-v5/few-shots/pair1-frieren.md`(★軸 A/B 該当時のみ★ 成語含む文学系の参考)
- `Read`: `.claude/skills/audio-lesson-v5/few-shots/pair2-conversation.md`(★軸 C/D/E 該当時のみ★ 会話文 + 語気助詞・量詞・アスペクトの参考)
- `Read`: `.claude/skills/audio-lesson-v5/few-shots/pair3-subtitle.md`(★軸 F/G 該当時のみ★ 字幕意訳 + 語順入替の参考)
- `Write`: 各モジュールを `/tmp/v5-pair{i}-phase{j}.xml` に書き出す(Step 6 の lint 入力にする)

### 成功条件
- 選択された各 Phase の `<speak>...</speak>` SSML が生成され、ファイルに書き出されている
- どの Few-shot を主参照したか(または該当ゼロで本体のみ参照したか)が **チャット応答** に明記されている

### 失敗時の対処
- SSML が `<speak>` で閉じていない → 即修正
- 生成中に鬼門ワード混入に気づいた → その場で代替表現に書き換え

### 真因対策
- **Q1/Q4(画数・鬼門ワード)対策**: 生成時に意識(ただし最終防衛は Step 6 の lint)
- **Q5(Issue 括弧ふりがな)対策**: Step 2 で抽出した辞書を Phase 1 + Phase 2 で必ず反映
- **Q6(機能語誤配置)対策**: 機能語を Phase 4 トラックに集約、Phase 0 対象外
- **「やらせない設計」(v1.5.0)**: Phase 2 を 3 択判定の固定テンプレに絞ることで、構成解説の自由度が生む字形精度問題(Q1)・Phase 2 空洞化(Q2)・印象表現テンプレ化(Q8)を構造的に消滅させる
- **v1.5.1 Few-shot 適用条件 7 軸**: 「該当ゼロなのに全 Few-shot 読む」「該当ありなのに参照しない」のあいまい判断を構造的に潰す(High #7 反映)
- **v1.5.1 Read 証跡の配置明確化**: チャット応答にのみ Read 履歴を出力、Issue コメントには出さない(High #8 反映)

---

## Step 6: lint 強制実行【スキル化の核心・★v1.5.1 改訂:二段化★】

### 二段化の趣旨(★v1.5.1 改訂:Medium #10 反映★)

v1.5.0 までは「モジュール生成直後の単体 lint」 1 段のみだったが、Phase 0↔Phase 2/3 照合(PATTERN 11)や効果音マーカー網羅(PATTERN 12)など **ペアまたぎの検証** はモジュール単体では捕捉できない。
v1.5.1 から **Step 6a(モジュール単独 lint)+ Step 6b(ペア全モジュール束ね --strict lint)** の二段に分割し、Step 6b を **Step 7 投稿直前の最終ゲート** として配置する。

```
Step 5 Phase j 生成
   ↓
Step 6a: モジュール単独 lint(デフォルト10パターン)
   ↓ exit 0
次の Phase へ
   ↓ ... 全 Phase 完了
   ↓
Step 6b: ペア全モジュール束ね --strict lint(12パターン+S5+S_FS+6C)
   ↓ exit 0
Step 7 投稿可
   ↓ exit ≠ 0
Step 7 投稿不可 → 該当モジュール再生成 → Step 6a → Step 6b リトライ
```

---

### Step 6a: モジュール単独 lint(各モジュール生成直後)

#### やること
1. Phase j の SSML ファイル(`/tmp/v5-pair{i}-phase{j}.xml`)に対して以下を実行:
   ```bash
   bash .claude/skills/audio-lesson-v5/scripts/v5-modular-lint.sh /tmp/v5-pair{i}-phase{j}.xml
   ```
2. 終了コード判定:
   - **exit 0** → そのモジュールは Step 6a 合格、次の Phase へ進む
   - **exit 1** → そのモジュールは不合格、**該当モジュールを再生成**(Step 5 のそのフェーズに戻る)
3. 再生成は最大 3 回まで。3 回失敗したら以下を実行:
   - lint 結果サマリを Issue に「現在難航中」コメントで投稿
   - ユーザー判断を仰ぐ

#### Step 6a の lint チェックパターン(デフォルト 10 種類)
| # | カテゴリ | 検出例 |
|---|---|---|
| 1 | 鬼門ワード(ルールE) | 「画/字義/然り而して/豈/几/疋/画数/N画/点がN個」 |
| 2 | 単独漢字裸出し(ルールA) | 地の文に「我」「帰」が単独で出る |
| 3 | 二度読み(ルールC) | 「我々(われわれ)」のような併記 |
| 4 | 繋辞 NG(v4 #3) | `</lang>です` `</lang>だ。` |
| 5 | Markdown 強調(v4 #10) | `**` `*` `__` `_` `` ` `` |
| 6 | 文法ポイント固定文化(短文+3固定) | 短文ペアで「文法ポイントは三つや」リテラル検出 |
| 6B | 文法ポイント逃げ表現(★v1.5.1新規★) | ホワイトリスト方式で「文法ポイント、見ていくで」のみ検出、正当な「2つや」素通し |
| 9 | リピート2回戻し(v1.6.0予定) | リピートが1回しかない |
| 10 | break 250ms 固定徹底 | 句読点直後の `<break time="500ms"/>` 等の例外 |
| - | その他既存パターン | (lint.sh 実装側で網羅) |

---

### Step 6b: ペア全モジュール束ね --strict lint(★v1.5.1 新規:投稿直前ゲート★)

#### やること
1. ペア i の全モジュール(Phase 1〜4 または選択分)が **Step 6a 全合格** した直後に実行
2. ペア i の全モジュールファイルを束ねて **--strict モード** で lint 実行:
   ```bash
   bash .claude/skills/audio-lesson-v5/scripts/v5-modular-lint.sh --strict /tmp/v5-pair{i}-phase1.xml /tmp/v5-pair{i}-phase2.xml /tmp/v5-pair{i}-phase3.xml /tmp/v5-pair{i}-phase4.xml
   ```
3. 終了コード判定:
   - **exit 0** → ペア i は Step 6b 合格、次のペアへ(または全ペア完了で Step 7 へ)
   - **exit 1** → **Step 7 投稿不可**、検出されたカテゴリに応じて該当モジュールを Edit / 再生成 → Step 6a 再実行 → Step 6b 再実行

#### Step 6b の追加 lint チェックパターン(--strict 専用 12+ 種類)

Step 6a の 10 パターンに加えて以下を検証:

| # | カテゴリ | 検出例 |
|---|---|---|
| 11 | Phase 0 ↔ Phase 2/3 不一致(★v1.5.1改訂★) | 横断表 4 語と Phase 2/3 詳細 5 列表の単語が不一致(二段検出) |
| 12 | 効果音マーカー網羅 | `<!-- explain -->` 個数 ≠ 期待ブロック数 |
| S5 | 5 列ヘッダ「選定理由」固定検証(★v1.5.1新規★) | `\| *# *\| *単語 *\| *ピンイン.* *\| *日本語訳 *\| *選定理由 *\|` 正規表現でヘッダ確認 |
| S_FS | (v1.5.1 撤回・実装しない) | Few-shot Read 証跡はチャット応答配置のため lint 検査不能、PATTERN_S_FS 撤回(High #8 反映) |
| 6C | 短文+3点固定(Phase 4 限定スコープ★v1.5.1新規★) | Phase 4 末尾リピート例文 9 codepoint 以下 +「ひとつ目」「ふたつ目」「みっつ目」全出現 |

**Step 6b 投稿直前ゲートの位置付け**:
- Step 6b の exit 0 が **Step 7 コメント投稿の必要条件**
- Step 6b 未実行・exit ≠ 0 状態での Step 7 進行は **絶対禁止**(構造的にブロック)
- 各ペアごとに Step 6b を完了させる(ペア間の lint 漏れ防止)

#### Step 6a/6b 共通: 内部メモ記録(義務)

ペア i の Step 6b パス確認後、ペア i の lint 結果サマリを内部メモに記録:

```
[v5-modular lint 結果]
ペア i Phase 1: Step 6a ✅ exit 0 (1回目) / Step 6b ✅ exit 0 (1回目)
ペア i Phase 2: Step 6a ⚠️ 鬼門ワード 1 hit → 再生成 → ✅ exit 0 (2回目) / Step 6b ✅ exit 0 (1回目)
ペア i Phase 3: Step 6a ✅ exit 0 (1回目) / Step 6b ✅ exit 0 (1回目)
ペア i Phase 4: Step 6a ✅ exit 0 (1回目) / Step 6b ✅ exit 0 (1回目)
```

#### 使うツール
- `Bash`: `bash .claude/skills/audio-lesson-v5/scripts/v5-modular-lint.sh <file>`(Step 6a)
- `Bash`: `bash .claude/skills/audio-lesson-v5/scripts/v5-modular-lint.sh --strict <file1> <file2> ...`(Step 6b)
- `Read`: lint 出力を読んで失敗カテゴリと該当行を把握
- `Edit`: 該当箇所をピンポイント修正(全モジュール再生成は最終手段)

#### 成功条件
- 全ペアの Step 6a + Step 6b が共に exit 0
- lint 結果サマリ(Step 6a/6b × N ペア × M モジュール)が内部メモに記録されている

#### 失敗時の対処
- Step 6a exit 1 → 該当モジュールを Edit で修正 → 再 lint(最大 3 回)
- Step 6b exit 1 → 検出パターンに応じて該当モジュールを再生成 → Step 6a パス確認 → Step 6b 再実行
- 3 回連続失敗 → Issue に難航コメント投稿、ユーザー判断仰ぐ

#### 真因対策
- **Q7(lint やってない告白)対策 = スキル化の核心**:
  - 別エージェントが「lint 実行するのを忘れてました」と告白した事案を構造的に潰す
  - Step 6a/6b が手順として組み込まれているため、Step 5 の次に **必ず** lint が走る
  - Step 6b exit 0 を確認しない限り Step 7(コメント投稿)に到達できない
- **Q1/Q4(鬼門ワード)対策の最終防衛線**: 生成時にスリップしても lint で必ず捕捉
- **v1.5.1 Step 6b 二段化**: ペアまたぎ検証(PATTERN 11/12/S5/6C)を「単体 lint では見えない盲点」として独立ゲート化(Medium #10 反映)

---

## Step 7: コメント投稿【★v1.5.1 改訂:横断表 + 詳細表 5 列・投稿直前 Step 6b 必須★】

### 投稿前提条件(★v1.5.1 新規:厳密化★)

Step 7 に進む前に、必ず以下を確認:

1. **全ペアの Step 6a + Step 6b が exit 0**(lint 二段とも合格)
2. lint 結果サマリ(Step 6a/6b × N ペア × M モジュール)が内部メモに記録済み
3. Few-shot 適用判定(Step 5 の 7 軸判定)が **チャット応答** に出力済み

上記いずれかが未充足の場合、**Step 7 進行不可**。Step 5/6 に戻る。

### やること

1. 全ペアの全モジュールの Step 6b パス確認後、Issue にコメントを投稿
2. **コメントサイズ制限**(★v1.5.1 改訂★):
   - 旧 v1.5.0:「1 コメント ≦ 4 ペア(16 ブロック)固定制限」
   - 新 v1.5.1:「1 コメント ≦ **60000 char(GitHub 上限 65536 のマージン)**」かつ「1 コメント ≦ 4 ペア」
   - 4 ペア未満でも 60000 char 超過なら強制分割
   - 5 ペア以上なら従来通り強制分割(「(1/2)」「(2/2)」マーカー)
3. **コメント Body の冒頭(★v1.5.1 改訂:横断簡易表 1 個に集約★)**:
   ```
   [Opus 4.7] djkinako/ssml-mp3-studio番頭

   **🔄 きなこ発言の要点:**
   - 例文ペア N 組
   - プロンプトモード: v5-modular
   - 出力モジュール: Phase 1/2/3/4(または選択分)
   - ソース種別: ...
   - 特別な指示: ...

   v5-modular で生成したで👇 ペアごとに Phase 1-4 を別 SSML として貼ってる。
   ssml-mp3-studio で **ペア数 × モジュール数 ブロックに貼って結合** すると 1 つの MP3 になる。

   ## フォーカス語サマリ(横断)

   | ペア | 日本語見出し | 中国語見出し | フォーカス語 1-4 |
   |---|---|---|---|
   | 1 | 君と僕は凱旋して帰る | 我們凱旋而歸 | 我們 / 凱旋 / 而 / 歸 |
   | 2 | 今日は傘持ってきたほうがいいで | 今天好像會下雨,帶把傘吧 | 今天 / 好像會 / 帶把傘 / 下雨 |
   ```

   **横断簡易表のフォーマット(★v1.5.1 新規★)**:
   - 4 列固定: `ペア / 日本語見出し / 中国語見出し / フォーカス語 1-4`
   - フォーカス語は ` / ` 区切りで列挙(4 語並べ)
   - 1 ペア時はこの横断表を **省略**、代わりに Bullet 表記:
     ```
     **フォーカス語:** 我們 / 凱旋 / 而 / 歸
     ```
   - 二重表問題(横断 + 詳細の冗長重複)解消(High #5 反映)

4. **2 階層見出し構造(★v1.5.1 改訂:ペアセクション冒頭は 5 列詳細表★)**:

   ```markdown
   ## ペア1: [日本語見出し] / [中国語見出し]

   **フォーカス語(Phase 0 サマリ・5 列詳細):**

   | # | 単語 | ピンインカタカナ | 日本語訳 | 選定理由 |
   |---|------|----------------|---------|----------|
   | 1 | 我們  | ウォーメン       | 私たち   | 繁体字特有「們」、人称複数の最頻出ワード、派生広い |
   | 2 | 凱旋  | カイセン         | 凱旋     | 難読「凱」、文のコア、日本語と共通熟語で覚えやすい |
   | 3 | 而    | アー             | しかして | 書面語マーカー、現代台湾華語の文章理解に必須 |
   | 4 | 歸    | グイ             | 帰る     | 繁体字の旧字体、口語「回」との対比軸 |

   ### Phase 1: ベース SSML
   ```xml
   <speak>...</speak>
   ```

   ### Phase 2: 漢字認識モジュール
   ```xml
   <speak>...</speak>
   ```

   ### Phase 3: 語彙深掘りモジュール
   ```xml
   <speak>...</speak>
   ```

   ### Phase 4: 文法モジュール
   ```xml
   <speak>...</speak>
   ```
   ```

   **5 列詳細表のフォーマット(★v1.5.1 改訂★)**:
   - 5 列固定: `# / 単語 / ピンインカタカナ / 日本語訳 / 選定理由`
   - 列名「選定理由」固定(「抽出根拠」「抽出理由」表記は撤回)
   - lint PATTERN_S5 (--strict) がこのヘッダを正規表現で検証

5. **末尾に MP3 化手順**:
   ```markdown
   ---

   #### ▶ MP3 化の手順 (v5-modular)

   1. https://djkinako.github.io/ssml-mp3-studio/ を開く
   2. 「+ ブロックを追加」を **{ペア数 × 選択モジュール数 - 1} 回押して** {合計} ブロックにする
   3. 各 `<speak>` を **1 個ずつ別ブロックに貼り付け**(順序: ペア1 Phase1 → ペア1 Phase2 → ... → ペアN Phase4)
   4. 「🔊 MP3 生成」 → 自動で 1 つの MP3 に結合 → ダウンロード
   ```

6. lint 結果サマリも末尾に付記(透明性、Q7 対策の見える化):
   - **v1.4.1 改訂: 必ず `<details>` で折りたたむ**(きなこは普段見ない、開きたい時だけ展開)
   - 折りたたみ化の真因対策: エージェントが「メタ報告だから画使ってもいい」と
     セーフゾーン認識して画字を混入させる事案(Issue #18)を物理的に消す
   - `<summary>` の中身に「lint 結果(N 回再生成)」など要約 1 行のみ。本文に画字を書く誘惑を絶つ
   - SUMMARY ラベルは数字+「再生成」だけで OK(本文の画字書き込みを避ける)
   - **v1.5.1 改訂: Step 6a/6b 二段の結果を併記**

   ```markdown
   <details>
   <summary>lint 結果(全モジュール OK 確認済み・Step 6a/6b 二段パス・再生成 N 回)</summary>

   | ペア | Phase1 6a/6b | Phase2 6a/6b | Phase3 6a/6b | Phase4 6a/6b |
   |---|---|---|---|---|
   | 1 | ✅/✅ (1/1回目) | ✅/✅ (2/1回目) | ✅/✅ (1/1回目) | ✅/✅ (1/1回目) |
   | 2 | ✅/✅ (1/1回目) | ✅/✅ (1/1回目) | ✅/✅ (1/1回目) | ✅/✅ (1/1回目) |

   </details>
   ```

   注意: 折りたたみ内本文に「画」「字義」「然り而して」等の鬼門ワードを書かないこと。
   v1.4.1 から lint.sh は .md メタ領域(xml ブロック以外)も画字検査するので、混入時は
   ルール E 違反として検出され投稿不可になる。

7. **Few-shot Read 証跡は Issue コメントに出さない(★v1.5.1 改訂:High #8 反映★)**:
   - Read 履歴(どの Few-shot を参照したか)は **チャット応答にのみ出力**
   - Issue コメント本文には含めない(きなこの読み物体験を最適化、メタ情報は別チャネル)

### 使うツール
- `Bash`: `gh issue comment <number> --body-file /tmp/v5-comment.md`
- `Write`: `/tmp/v5-comment.md` にコメント本文を書き出す(HEREDOC エスケープ事故防止)

### 成功条件
- Issue にコメント投稿成功(`gh issue comment` の exit 0)
- 投稿後 `gh issue view <number> --comments` で末尾コメントが意図通りであることを目視確認
- チャット応答にコメント URL(`https://github.com/.../issues/N#issuecomment-XXXX`)を明記してユーザーに返す
- チャット応答に Few-shot Read 証跡(どの軸該当 → どの Few-shot 参照)を出力

### 失敗時の対処
- `gh issue comment` 失敗 → 認証 / リポジトリ確認、再試行
- 投稿後の確認で SSML が崩れている(コードブロックエスケープ事故) → 該当コメントを `gh api ... -X PATCH` で編集修正
- コメントが 60000 char 超過 → 強制分割(ペア単位で分け、(1/2)(2/2) マーカー付与)

### 真因対策
- **グローバル CLAUDE.md ルール完遂**:
  - `[モデル名] djkinako/ssml-mp3-studio番頭` プレフィックス
  - `🔄 きなこ発言の要点` bullet 要約
  - チャット応答に投稿 URL 明記
- **Q7(透明性)対策**: lint 結果サマリをコメント末尾に付記してきなこに見せる
- **v1.5.1 横断表+詳細表分離**: 二重持ち問題を解消、横断表で全体俯瞰 → 各ペアセクションで深掘り(High #5 反映)
- **v1.5.1 列名「選定理由」統一**: 4 ファイル間表記揺れを撲滅(Critical #3 反映)
- **v1.5.1 Step 6b 投稿直前ゲート**: ペアまたぎ検証を未済のまま投稿する事案を構造的に潰す(Medium #10 反映)

---

## まとめ:スキル化の核心メリット

1. **Step 6a/6b の二段 lint 強制が手順として組み込まれる(★v1.5.1 改訂★)**
   - Step 6a: モジュール生成直後の単体 lint(デフォルト 10 パターン)
   - Step 6b: ペア全モジュール束ね --strict lint(12 + S5 + 6C パターン、投稿直前ゲート)
   - エージェントが「面倒で省く」リスクを構造的に潰す
   - 別エージェント Q7 「やってない」告白の解決

2. **Step 2/Step 5 の Issue 括弧ふりがな → `<sub alias>` 強制**
   - Q5「Issue で指定したのに反映されない」の解決

3. **Step 4 の自己相互チェック 3 点 + サマリ表 5 列「選定理由」固定出力(★v1.5.1 改訂★)**
   - Q2(微修正)/ Q3(分割ブレ)/ Q6(機能語誤配置)の解決
   - 列名「選定理由」で 4 ファイル統一(Critical #3 反映)

4. **Step 5 Few-shot 適用条件 7 軸明文化(★v1.5.1 新規★)**
   - 「該当ゼロなのに全部読む」「該当ありなのに参照しない」あいまい判断を構造的に潰す(High #7 反映)
   - Read 証跡はチャット応答にのみ出力、Issue コメントには出さない(High #8 反映)

5. **Step 5 Phase 2 の 3 パターン判定固定テンプレ(v1.5.0)**
   - エージェント裁量ゼロ化:「日本語と同じ / 旧字体 / 繁体字特有」の 3 択のみ
   - 構成解説(部首・部品)・印象表現・字形描写を構造的に削除
   - Q1(画数表記スリップ)/ Q2(Phase 2 空洞化)/ Q8(印象表現テンプレ化)の構造的解決
   - 真の責務:音声学習で「あ、あの字ね」と即座に思い出せる **認識** の提供(書取は紙とペンで)

6. **Step 7 の横断表+詳細表分離 + 5 列「選定理由」 + bullet 要約(★v1.5.1 改訂★)**
   - 横断簡易表(4 列・ペア横断)で全体俯瞰
   - ペアセクション冒頭に詳細 5 列表(「選定理由」固定)で深掘り
   - 二重持ち問題解消(High #5 反映)
   - 既存のグローバルルール + プロジェクトルール完全準拠

このスキルは **「決定論的手順 + 自動検証(lint 二段)」** を構造で強制することで、別エージェント検証で表面化した Q1-Q8 の構造的失敗パターンを全て解決する。
